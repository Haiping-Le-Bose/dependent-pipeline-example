steps:
  - command: echo 'Tests'
    label: ':hammer:'
  - wait
  - trigger: dummy-deploy
    label: ":rocket:"
    branches: "main"
    async: false
    build:
      commit: "$BUILDKITE_COMMIT"
      branch: "$BUILDKITE_BRANCH"
      env:
        release-version: "1.1"
